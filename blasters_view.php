<?php
// This script and data application were generated by AppGini 5.95
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/blasters.php");
	include_once("{$currDir}/blasters_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('blasters');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout(function() { window.location = "index.php?signOut=1"; }, 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'blasters';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`blasters`.`manufacturer`" => "manufacturer",
		"if(`blasters`.`date`,date_format(`blasters`.`date`,'%m/%d/%Y'),'')" => "date",
		"`blasters`.`blaster_id`" => "blaster_id",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => 1,
		2 => '`blasters`.`date`',
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`blasters`.`manufacturer`" => "manufacturer",
		"if(`blasters`.`date`,date_format(`blasters`.`date`,'%m/%d/%Y'),'')" => "date",
		"`blasters`.`blaster_id`" => "blaster_id",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`blasters`.`manufacturer`" => "Manufacturer",
		"`blasters`.`date`" => "Date",
		"`blasters`.`blaster_id`" => "Blaster id",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`blasters`.`manufacturer`" => "manufacturer",
		"if(`blasters`.`date`,date_format(`blasters`.`date`,'%m/%d/%Y'),'')" => "date",
		"`blasters`.`blaster_id`" => "blaster_id",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`blasters` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'blasters_view.php';
	$x->RedirectAfterInsert = 'blasters_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Blasters';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`blasters`.`blaster_id`';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Manufacturer', 'Date', 'Blaster id', ];
	$x->ColFieldName = ['manufacturer', 'date', 'blaster_id', ];
	$x->ColNumber  = [1, 2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/blasters_templateTV.html';
	$x->SelectedTemplate = 'templates/blasters_templateTVS.html';
	$x->TemplateDV = 'templates/blasters_templateDV.html';
	$x->TemplateDVP = 'templates/blasters_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `blasters`.`blaster_id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='blasters' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `blasters`.`blaster_id`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='blasters' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`blasters`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: blasters_init
	$render = true;
	if(function_exists('blasters_init')) {
		$args = [];
		$render = blasters_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: blasters_header
	$headerCode = '';
	if(function_exists('blasters_header')) {
		$args = [];
		$headerCode = blasters_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: blasters_footer
	$footerCode = '';
	if(function_exists('blasters_footer')) {
		$args = [];
		$footerCode = blasters_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
