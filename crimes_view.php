<?php
// This script and data application were generated by AppGini 5.95
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/crimes.php");
	include_once("{$currDir}/crimes_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('crimes');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied'], false);
		echo '<script>setTimeout(function() { window.location = "index.php?signOut=1"; }, 2000);</script>';
		exit;
	}

	$x = new DataList;
	$x->TableName = 'crimes';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`crimes`.`type`" => "type",
		"if(`crimes`.`date`,date_format(`crimes`.`date`,'%m/%d/%Y'),'')" => "date",
		"`crimes`.`file_num`" => "file_num",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => 1,
		2 => '`crimes`.`date`',
		3 => 3,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`crimes`.`type`" => "type",
		"if(`crimes`.`date`,date_format(`crimes`.`date`,'%m/%d/%Y'),'')" => "date",
		"`crimes`.`file_num`" => "file_num",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`crimes`.`type`" => "Type",
		"`crimes`.`date`" => "Date",
		"`crimes`.`file_num`" => "File num",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`crimes`.`type`" => "type",
		"if(`crimes`.`date`,date_format(`crimes`.`date`,'%m/%d/%Y'),'')" => "date",
		"`crimes`.`file_num`" => "file_num",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`crimes` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'crimes_view.php';
	$x->RedirectAfterInsert = 'crimes_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Crimes';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`crimes`.`file_num`';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Type', 'Date', 'File num', ];
	$x->ColFieldName = ['type', 'date', 'file_num', ];
	$x->ColNumber  = [1, 2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/crimes_templateTV.html';
	$x->SelectedTemplate = 'templates/crimes_templateTVS.html';
	$x->TemplateDV = 'templates/crimes_templateDV.html';
	$x->TemplateDVP = 'templates/crimes_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// mm: build the query based on current member's permissions
	$DisplayRecords = $_REQUEST['DisplayRecords'];
	if(!in_array($DisplayRecords, ['user', 'group'])) { $DisplayRecords = 'all'; }
	if($perm['view'] == 1 || ($perm['view'] > 1 && $DisplayRecords == 'user' && !$_REQUEST['NoFilter_x'])) { // view owner only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `crimes`.`file_num`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='crimes' AND LCASE(`membership_userrecords`.`memberID`)='" . getLoggedMemberID() . "'";
	} elseif($perm['view'] == 2 || ($perm['view'] > 2 && $DisplayRecords == 'group' && !$_REQUEST['NoFilter_x'])) { // view group only
		$x->QueryFrom .= ', `membership_userrecords`';
		$x->QueryWhere = "WHERE `crimes`.`file_num`=`membership_userrecords`.`pkValue` AND `membership_userrecords`.`tableName`='crimes' AND `membership_userrecords`.`groupID`='" . getLoggedGroupID() . "'";
	} elseif($perm['view'] == 3) { // view all
		// no further action
	} elseif($perm['view'] == 0) { // view none
		$x->QueryFields = ['Not enough permissions' => 'NEP'];
		$x->QueryFrom = '`crimes`';
		$x->QueryWhere = '';
		$x->DefaultSortField = '';
	}
	// hook: crimes_init
	$render = true;
	if(function_exists('crimes_init')) {
		$args = [];
		$render = crimes_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: crimes_header
	$headerCode = '';
	if(function_exists('crimes_header')) {
		$args = [];
		$headerCode = crimes_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: crimes_footer
	$footerCode = '';
	if(function_exists('crimes_footer')) {
		$args = [];
		$footerCode = crimes_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
